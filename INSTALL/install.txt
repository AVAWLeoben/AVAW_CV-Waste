ANACONDA ENVIRONMENT ROUTE:
conda create -n AVAW_BBOX python=3.12.11
python -m pip install -U pip
python -m pip install -r requirements.txt

FIXED PIP VERSIONS ANACONDA ENVIRONMENT ROUTE:
conda create -n AVAW_BBOX python=3.12.11
python -m pip install -U pip
python -m pip install -r requirements-lock.txt

ANACONDA ENVIRONMENT RECREATION ROUTE
conda env create -f environment.yml

OR
conda env create -f environment-portable.yml

OR
conda env create -f environment-from-history.yml